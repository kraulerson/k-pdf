# Implicit Feature: Undo/Redo — Implementation Plan

**Date:** 2026-04-02
**Spec:** `docs/superpowers/specs/2026-04-02-undo-redo-design.md`
**Branch:** `feature/undo-redo`
**Depends on:** Features 1, 2, 6, 7, 8, 9 (all complete)

---

## Task Overview

| # | Task | Type | Files |
|---|------|------|-------|
| 1 | UndoAction dataclass + UndoManager core | core | `k_pdf/core/undo_manager.py`, `tests/test_undo_manager.py` |
| 2 | TabContext integration | presenter | `k_pdf/presenters/tab_manager.py`, `tests/test_tab_manager.py` |
| 3 | MainWindow Edit menu: Undo/Redo actions | view | `k_pdf/views/main_window.py`, `tests/test_views.py` |
| 4 | KPdfApp wiring + integration tests | app | `k_pdf/app.py`, `tests/test_undo_integration.py` |
| 5 | Mypy overrides + CLAUDE.md update | config | `pyproject.toml`, `CLAUDE.md` |

---

## Task 1: UndoAction + UndoManager

### RED — Write failing tests

**File: `tests/test_undo_manager.py`**

```python
class TestUndoAction:
    def test_construction(self): ...
    def test_description_required(self): ...

class TestUndoManager:
    def test_initial_state_empty(self): ...
    def test_push_makes_can_undo_true(self): ...
    def test_undo_calls_undo_fn(self): ...
    def test_redo_calls_redo_fn(self): ...
    def test_push_clears_redo_stack(self): ...
    def test_stack_size_limit(self): ...
    def test_undo_empty_is_noop(self): ...
    def test_redo_empty_is_noop(self): ...
    def test_clear(self): ...
    def test_descriptions(self): ...
    def test_state_changed_signal(self): ...
```

### GREEN — Implement

**File: `k_pdf/core/undo_manager.py`** — Replace stub with UndoAction dataclass and UndoManager class.

### REFACTOR — None needed.

---

## Task 2: TabContext Integration

### RED — Write failing tests

**File: `tests/test_tab_manager.py`** (extend existing)

```python
class TestTabManagerUndo:
    def test_tab_context_has_undo_manager(self): ...
    def test_get_active_undo_manager_returns_instance(self): ...
    def test_get_active_undo_manager_no_active_tab(self): ...
    def test_force_close_tab_discards_undo_manager(self): ...
```

### GREEN — Implement

**File: `k_pdf/presenters/tab_manager.py`** — Add UndoManager to TabContext, add `get_active_undo_manager()` method.

### REFACTOR — None needed.

---

## Task 3: MainWindow Undo/Redo Menu

### RED — Write failing tests

**File: `tests/test_views.py`** (extend existing)

```python
class TestUndoRedoMenu:
    def test_undo_action_exists(self): ...
    def test_redo_action_exists(self): ...
    def test_undo_shortcut(self): ...
    def test_redo_shortcut(self): ...
    def test_undo_initially_disabled(self): ...
    def test_redo_initially_disabled(self): ...
    def test_set_undo_state(self): ...
    def test_undo_triggered_signal(self): ...
    def test_redo_triggered_signal(self): ...
```

### GREEN — Implement

**File: `k_pdf/views/main_window.py`** — Add Undo/Redo QActions to Edit menu, signals, set_undo_state() method.

### REFACTOR — None needed.

---

## Task 4: KPdfApp Wiring + Integration

### RED — Write failing tests

**File: `tests/test_undo_integration.py`**

```python
class TestUndoRedoIntegration:
    def test_undo_calls_active_undo_manager(self): ...
    def test_redo_calls_active_undo_manager(self): ...
    def test_tab_switch_updates_undo_state(self): ...
    def test_undo_manager_state_change_updates_menu(self): ...
    def test_undo_when_no_tab_is_noop(self): ...
```

### GREEN — Implement

**File: `k_pdf/app.py`** — Wire undo/redo signals, connect UndoManager.state_changed per tab.

### REFACTOR — None needed.

---

## Task 5: Config + Documentation

- Add mypy override for `k_pdf.core.undo_manager` if needed
- Update `CLAUDE.md` current state section
